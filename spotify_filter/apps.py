from django.apps import AppConfig


class SpotifyFilterConfig(AppConfig):
    """Configuration for spotify_filter app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "spotify_filter"
